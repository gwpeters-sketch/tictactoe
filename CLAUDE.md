# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Workflow

Every new project gets its own GitHub repo. After creating or significantly changing a project:
1. Commit with a clear message describing what changed and why
2. Push to the corresponding GitHub remote

Git is configured globally as Gareth / gwpeters@gmail.com (GitHub: gwpeters-sketch).

## Projects

### tictactoe.html
A single-file browser game — all HTML, CSS, and JS are inline in one file. No build step, no dependencies. Open directly in a browser to play.

- Two-player (X and O on the same screen)
- Tracks scores across games
- Highlights winning cells
